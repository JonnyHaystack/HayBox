name: PlatformIO CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Cache PlatformIO
      uses: actions/cache@v2
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}

    - name: Set up Python
      uses: actions/setup-python@v2

    - name: Install PlatformIO
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade platformio

    - name: Build Pico
      run: pio run -e pico

    - name: Organize files for upload
      shell: bash
      run: |
        mkdir -p picofw
        cp .pio/build/pico/firmware.uf2 picofw/pico.uf2

    - name: Publish pico artifacts
      uses: actions/upload-artifact@v2
      with:
        name: Pico firmware
        path: picofw

    - name: Build Arduino Micro
      run: pio run -e uno

    - name: Organize files for upload
      shell: bash
      run: |
        mkdir -p unofw
        cp .pio/build/uno/firmware.hex unofw/uno.hex

    - name: Publish uno artifacts
      uses: actions/upload-artifact@v2
      with:
        name: Uno firmware
        path: unofw
